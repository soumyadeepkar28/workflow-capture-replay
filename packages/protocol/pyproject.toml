[project]
name = "workflow-protocol"
version = "0.1.0"
description = "Shared validated contracts for workflow capture and replay"
requires-python = ">=3.13,<3.14"
dependencies = [
    "pydantic>=2.11,<3",
]

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"