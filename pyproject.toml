[project]
name = "workflow-capture-replay"
version = "0.1.0"
description = "Browser workflow capture and replay prototype"
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.pytest.ini_options]
addopts = "-q"
testpaths = [
	"apps/api/tests",
	"apps/companion/tests",
	"packages/protocol/tests",
	"packages/target-state/tests",
]